let number = 1; // Start with the first number

while (number <= 5) {
    const square = number * number; // Calculate the square
    console.log(`The square of ${number} is ${square}`);
    number++; // Increment the number
}
